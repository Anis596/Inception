[www]
user = www-data
group = www-data
; on écoute le port 9000
listen = 9000

; Les réglages standards pour allouer de la mémoire à PHP
pm = dynamic
pm.max_children = 25
pm.start_servers = 5
pm.min_spare_servers = 1
pm.max_spare_servers = 10
